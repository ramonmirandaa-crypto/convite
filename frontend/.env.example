# ============================================
# SUPABASE + VERCEL INTEGRATION
# ============================================
# Estas variáveis são configuradas automaticamente
# pela integração Vercel + Supabase
#
# Para desenvolvimento local, copie de:
# Supabase Dashboard > Settings > Database > Connection string

# URL para Prisma (com pooling - recomendado para serverless)
POSTGRES_PRISMA_URL=

# URL direta (sem pooling - para migrations)
POSTGRES_URL_NON_POOLING=

# URLs alternativas (também funcionam)
POSTGRES_URL=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Supabase Auth (se precisar usar Auth do Supabase)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SUPABASE_URL=

# ============================================
# CONFIGURAÇÕES DO APLICATIVO
# ============================================

# Chave de encriptação (gerar com: openssl rand -base64 32)
ENCRYPTION_KEY=change-this-to-a-strong-random-key-in-production

# Credenciais de admin para área restrita
ADMIN_USER=admin
ADMIN_PASSWORD=admin123

# Ambiente
NODE_ENV=production
