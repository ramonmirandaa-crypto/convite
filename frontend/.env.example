# Banco de dados PostgreSQL (use Supabase ou Neon para gratuito na nuvem)
# Exemplo Supabase: postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres
# Exemplo Neon: postgresql://[USER]:[PASSWORD]@[HOST]/[DB]?sslmode=require
DATABASE_URL=

# Chave de encriptação (gerar uma chave forte para produção)
# Exemplo: openssl rand -base64 32
ENCRYPTION_KEY=change-this-to-a-strong-random-key-in-production

# Credenciais de admin para área restrita
ADMIN_USER=admin
ADMIN_PASSWORD=admin123

# Ambiente
NODE_ENV=production
